ultralytics>=8.3.0
supervision>=0.25.0
opencv-python>=4.10.0
numpy>=1.26.0
lap>=0.5.12

# stage_c_numbers — PARSeq via torch.hub (only OCR engine kept)
pytorch_lightning>=2.0
timm>=0.9
nltk>=3.8

# stage_d_entities — OSNet via torchreid (Re-ID for entity merging)
torchreid>=0.2.5
gdown>=5.0           # torchreid-transitive, not declared by it
tensorboard>=2.0     # torchreid-transitive, not declared by it
