tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.1
new_track_thresh: 0.25
track_buffer: 180        # keep lost tracks alive 3s @ 60fps instead of 0.5s
match_thresh: 0.9        # more permissive association — fewer new IDs
fuse_score: True
